server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /{{ cookiecutter.api_prefix }} {
        proxy_pass http://backend-service:8000;
        proxy_set_header Host $host;
    }
}
