apiVersion: v2
name: my-full-stack-app
description: Helm chart for deploying a Django backend and React frontend CV application. Supports Traefik and Nginx ingress controllers with cert-manager TLS.
version: 0.1.0
appVersion: "1.0"
type: application
